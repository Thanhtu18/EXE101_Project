// Provide minimal runtime stubs for previously TypeScript-only exports
// so modules that import named types (from './types') do not fail at runtime.
export const RentalProperty = {};
export const LandlordProfile = {};

// Default export kept for compatibility
export default {};
